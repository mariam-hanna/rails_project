<h1>Listing hotels</h1>

<table>
  <tr>
    <th>Country</th>
    <th>City</th>
    <th>Region</th>
    <th>Name</th>
    <th>Address</th>
    <th>Phone</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @hotels.each do |hotel| %>
  <tr>
    <td><%= hotel.country %></td>
    <td><%= hotel.city %></td>
    <td><%= hotel.region %></td>
    <td><%= hotel.name %></td>
    <td><%= hotel.address %></td>
    <td><%= hotel.phone %></td>
    <td><%= link_to 'Show', hotel %></td>
    <td><%= link_to 'Edit', edit_hotel_path(hotel) %></td>
    <td><%= link_to 'Destroy', hotel, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Hotel', new_hotel_path %>
